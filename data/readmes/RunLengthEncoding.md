# Run-Length Encoding (RLE)

Simple Java implementation demonstrating Run-Length Encoding (RLE). The README explains algorithm theory, usage instructions, and example output.

How to run:
- Compile `RunLengthEncoding.java` with `javac` and run with `java`.

Example output and explanation included in the README.